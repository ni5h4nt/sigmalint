title: FP001 negative - two distinct selectors
# Rule requires `len(_selectors()) == 1`. Two distinct non-filter
# selectors short-circuit the check.
logsource:
    category: process_creation
detection:
    selection:
        Image: cmd
    other_selection:
        CommandLine: foo
    condition: selection or other_selection
