title: FP003 positive - `not 1 of them` is not an exclusion selector
logsource:
    category: process_creation
detection:
    selection:
        Image|endswith: '\reg.exe'
    filter_admin:
        User|startswith: 'NT AUTHORITY\'
    condition: not 1 of them
