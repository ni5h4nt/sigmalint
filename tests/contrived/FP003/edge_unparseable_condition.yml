title: FP003 edge - condition string is malformed and fails to parse
# The rule wraps `parse(condition)` in try/except ConditionParseError
# and returns silently. This fixture exercises that swallow.
logsource:
    category: process_creation
detection:
    selection:
        Image: cmd.exe
    condition: "selection and )("
