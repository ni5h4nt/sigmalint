title: FP002 positive - list-of-dict, one item with wildcard (walker gap pre-fix)
# Pre-fix: walker drops list-of-dict; the rule does not iterate this
# shape and produces 0 findings - test fails.
# Post-fix: _selectors_iter yields one (name, body) tuple; FP002
# iterates per-tuple and fires on the wildcard inside the body's dict.
logsource:
    category: process_creation
detection:
    selection:
        - CommandLine: "*encoded*"
    condition: selection
