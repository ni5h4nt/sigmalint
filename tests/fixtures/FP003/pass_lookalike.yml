title: FP003 lookalike process_creation with negated filter
logsource:
    category: process_creation
detection:
    selection:
        Image|endswith: \powershell.exe
        CommandLine|contains: suspicious-arg
    filter:
        CommandLine|contains: trusted-tool
    condition: selection and not filter
